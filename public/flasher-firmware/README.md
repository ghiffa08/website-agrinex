# AgriNex Web Flasher - Firmware Binaries

Folder ini berisi firmware binaries yang sudah di-compile untuk ESP32 Web Flasher.

## Struktur Folder

```
flasher-firmware/
├── sender/
│   ├── firmware.bin
│   ├── bootloader.bin
│   ├── partitions.bin
│   ├── boot_app0.bin
│   └── manifest.json
├── receiver/
│   ├── firmware.bin
│   ├── bootloader.bin
│   ├── partitions.bin
│   ├── boot_app0.bin
│   └── manifest.json
└── tester/
    ├── firmware.bin
    ├── bootloader.bin
    ├── partitions.bin
    ├── boot_app0.bin
    └── manifest.json
```

## Rebuild Firmware

Untuk rebuild firmware dari source code:

```bash
cd /path/to/Projects
python deploy.py
```

Script ini akan:
1. Build semua firmware menggunakan PlatformIO
2. Copy binaries ke folder ini
3. Generate manifest.json untuk ESP Web Tools

## Flash Offset Map

- `bootloader.bin` → 0x1000
- `partitions.bin` → 0x8000
- `boot_app0.bin` → 0xe000
- `firmware.bin` → 0x10000

## Web Flasher URL

Akses web flasher di:
https://smartdrip-system.agrinex.io/admin/flasher

## Requirements

- Chrome, Edge, atau Opera browser (Web Serial API support)
- ESP32-S3 connected via USB-C
- Admin atau Operator role

---
Auto-generated by build-flasher-firmware.py
